- HTML5语义化标签
```
header 标签
article 标签  规定独立的自包含内容
fieldset 标签 组合表单中的相关元素：
section 标签  文档中的区段
figure 标签   用作文档中插图的图像 
figcaption 标签 用作文档中插图的图像，带有一个标题
```

```
css 选择器 [] > nth-of-type nth-last-of-type 
form 表单 
```
